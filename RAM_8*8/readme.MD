in ram 8*8  there is 8 location and each loaction have 8 bits 
now we have signal write enable which act as if that is 0 means  in will work in read mode and 
when write enable is 1 in this case in will write the data into the ram 
now for writing the data we have to give write address means where to write the data 
 after that we give data by data_in input 
and to read the data from the output data_out we have to give address , which adress data we want to read for that we use input rd_addr
